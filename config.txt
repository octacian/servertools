-- servertools config --

-- genesis module (like static spawn, default: true)
genesis = true

-- misc commands module (default: true)
misc = true

-- chat filter (default: true)
filter = true

----- RANKING SYSTEM -----
-- enable ranking (default: true)
rank = true

-- rank chat prefixes (default: true)
rank_chat = true

-- rank specific commands (default: true)
rank_specific_commands = true

----- WHITELIST -----
-- enable whitelist (default: false)
whitelist = true
